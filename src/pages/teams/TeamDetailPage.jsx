import Wrapper from '@/components/wrappers/Wrapper';

const TeamDetailPage = () => {
    return (
        <Wrapper
            title={'Team Detail'}
            description={'Menu ini digunakan untuk melihat detail team'}
        >
            <h1>Team Detail Page</h1>
        </Wrapper>
    );
};

export default TeamDetailPage;
