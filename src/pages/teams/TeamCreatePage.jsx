import Wrapper from '@/components/wrappers/Wrapper';

const TeamCreatePage = () => {
    return (
        <Wrapper
            title={'Team Create'}
            description={'Menu ini digunakan untuk membuat team'}
        >
            <h1>Team Create Page</h1>
        </Wrapper>
    );
};

export default TeamCreatePage;
